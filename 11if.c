#include<stdio.h>
int main(){
	printf("Now we are trying if loop\n");
	int naren;
	printf("Enter a number : ");
	scanf("%d",&naren);
	if(naren<10){
		printf("It is a one digit number");
	}
}
